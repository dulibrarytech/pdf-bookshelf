<%/* An error notice for a non-table fragment target: the add-user form's
     message slot, the utilities report, the bookshelf table area. Escaped
     here for the same reason as message-row.ejs. */%>
<div class="alert alert-danger mb-0"><%= message %></div>
